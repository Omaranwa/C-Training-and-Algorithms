#include <stdio.h>

int main()
{
    // have the required info
    int length = 5;
    int width = 6;

    int area;

    // calculate the result
    area = length * width;

    // show the result
    printf("length is: %d\n", length);
    printf("Width is: %d\n", width);

    printf("area of rectangle is: %d\n", area);
}